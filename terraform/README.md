# terraform/

Infrastructure as Code for the local lab environment and cloud lift-and-shift.

Describes networks, resources, variables, and modules. State backend is remote — not a local file. Cloud target is a single provider (TBD at Layer 8). Cloud resources are destroyed after validation unless cost is negligible.

**Introduced:** Layer 8

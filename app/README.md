# app/

FastAPI application serving the feyBank payment API.

This is a pre-existing open-source demo, not written from scratch. Modifications are limited to what the infrastructure requires: `/health` and `/metrics` endpoints, and alignment with the PostgreSQL schema defined in `docker/`.

Do not treat this directory as an engineering subject. Changes here are a last resort.

**Introduced:** Layer 0
